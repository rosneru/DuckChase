{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Make project",
            "command": "/usr/bin/make",
            "args": [
                "debug",
                "-j3"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
