; project configuration (do not delete)

(compiler

    (alias "vbcc/classic")

    (makefile "makefiles/vbcc-classic/makefile")

    (make "make -f %makefile %target")

    (executable "bin/vbcc-classic/DuckChase_c")

    (stack "262144")

    (target "")

    (arguments "")

    (options

        "-c99"

        "-lauto"

        "-lamiga"

        "-lmsoft"
    )
)

; project configuration (do not delete)
